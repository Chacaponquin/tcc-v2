import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Crystal Chapel | Information",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
